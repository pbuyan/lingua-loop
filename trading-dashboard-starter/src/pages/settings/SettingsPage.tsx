import { PageHeader } from '@/shared/components/page-header/PageHeader';

export function SettingsPage() {
  return (
    <div className="page-grid">
      <PageHeader title="Settings" subtitle="Preferences, entitlements, and environment settings" />
      <div className="panel">Starter placeholder for settings.</div>
    </div>
  );
}
